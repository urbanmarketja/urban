# Database

This folder holds the database design artifacts for Urban Market JA.

- `schema.sql`: MySQL schema for the marketplace database.

The backend migration script currently reads `Backend/schema.sql`. Keep `database/schema.sql` and `Backend/schema.sql` in sync until migrations are moved into this folder.
